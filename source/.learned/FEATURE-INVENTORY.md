# FEATURE-INVENTORY — completeness catalog across 12 sources

> **Purpose:** ensure no good feature is missed (R30 of the 10-round completeness pass). Lean format: `name | 1-line desc | source`. The SPEC (`spec/`) stays focused on CORE; this file catalogs the rest so nothing is silently dropped.
> **Convention:** 🟥 = CORE feature missed/under-specified → fold into spec (R31+). 🟦 = breadth → candidate phase-2 package. Sources: CC=claw-code, HE=hermes-agent, OH=oh-my-pi, PI=pi-coding-agent, OC=openclaw, OU=openhuman, HR=headroom, OV=OpenViking, MA=MyAgents.

## Part 1 — 🟥 CORE features the SPEC under-specifies (fold in R31+)

These are load-bearing mechanics a Tier-0/1 builder needs but the spec only gestures at:

| Feature | 1-line | Source |
|---|---|---|
| **ToolSearch / deferrable tools** | long-tail tools kept out of prompt until BM25-queried or `select:`-activated; lazy activation when budget exceeded | HE `tools/tool_search.py`, CC `ToolSearch` |
| **TodoWrite / plan-mode tools** | structured per-session task list + EnterPlanMode/ExitPlanMode toggle (worktree-level) | CC `TodoWrite`/`EnterPlanMode`, PI plan skill |
| **Approval-Token ledger** | delegated-approval scopes w/ repository+branch, consume/verify/expiry, multi-hop chain — beyond a one-shot `ApprovalChannel` | CC `approval_tokens.rs` |
| **RecoveryRecipe FSM** | typed `FailureScenario`→`RecoveryStep`→`EscalationPolicy` recipe table (vs generic "bounded retry") | CC `recovery_recipes.rs` |
| **GreenContract (merge gate)** | 4-level green gate (TargetedTests/Package/Workspace/MergeReady) + evidence — subagent merge contract | CC `green_contract.rs` |
| **Project-trust (input-loading guard)** | `trust.json`; before trust only context-files + global `-e` extensions load; `defaultProjectTrust: ask\|always\|never` | PI `trust-manager.ts` |
| **Message queue (steer/followUp/nextTurn)** | queue messages mid-turn; `Alt+Enter` queues, `Alt+Up` restores; delivery modes | PI `agent-session.ts` |
| **`!cmd` / `!!cmd` editor prefixes** | `!` run+send to LLM; `!!` run+exclude; per-message BashExecutionMessage | PI `bash.ts` |
| **BashOperations delegation hook** | pluggable shell backend; `user_bash` event can supply custom ops/result | PI `bash.ts`, CC |
| **File-mutation queue** | serialize write/edit (`withFileMutationQueue`); per-tool `details.diff`+`details.patch`; `fullOutputPath` for big output | PI `file-mutation-queue.ts` |
| **Path-safety resolver (lexical vs canonical)** | `resolve_inside_workspace` (write, lexical) vs `resolve_existing_inside_workspace` (read, canonicalize+symlink-escape block) — real path-traversal defense | MA `workspace_files/path_safety.rs` |
| **Session JSONL tree + entry types** | tree-structured (`id`/`parentId`); entry types (message/model_change/compaction/branch_summary/custom/label…); v1→v2→v3 migration | PI `session-manager.ts`, CC `session.rs` |
| **Overflow-recovery compaction** | on `context_length_exceeded` stop: drop failed msg, compact, retry once | PI `compaction/`, CC |
| **Provider compat flags (~20)** | supportsDeveloperRole, thinkingFormat (qwen/deepseek/openrouter…), cacheControlFormat, requiresToolResultName, maxTokensField | PI `custom-provider.md`, CC |
| **Auth-profile rotation + cooldown + failover** | per-provider OAuth; cooldown auto-expiry; round-robin (not last-good); failover-error classification | OC `auth-profiles*` |
| **OAuth/PKCE loopback + token refresh** | `OAuthAuthorizationRequest`, PKCE `code_challenge_s256`, loopback redirect, refresh | CC `oauth.rs` |
| **prompt_cache_key strategy** | per-provider cache-key injection (Anthropic cache_control, OpenAI prompt_cache_key) | HR `openai_cache_key.rs`, OC |
| **Transport selection (SSE/WebSocket)** | `transport: auto\|sse\|websocket\|websocket-cached` + idle/connect timeouts | PI `sdk.ts` |
| **Settings merge (project-overrides-global) + lockfile** | global+project nested merge; `proper-lockfile` writes | PI `settings-manager.ts`, CC `config.rs` (5-file merge) |
| **Readiness 3-phase probe** | `/health/live` (liveness) vs `/ready` (readiness, 503+Retry-After) vs `/functional`; `DeferredInitState` | MA `readiness-state.ts` |
| **Unified cancel protocol** | `CancelReason = user\|timeout\|upstream\|shutdown\|error`; `AbortSignal.any` polyfill; cancellable fetch/delay | MA `cancellation.ts` |
| **Large-value spill (maybeSpill)** | payload >256KB → `~/.<app>/refs/<id>` + `LargeValueRef{preview,mimetype,ttl}`; SSE 3-tier priority | MA `large-value-store.ts` |
| **Context-window preflight** | reject requests where est. input+output > context_window w/ typed error | CC `preflight_message_request` |
| **CompressionPolicy per-auth-mode** | PAYG/OAuth/Subscription policy split (live_zone_only, cache_aligner, max_lossy_ratio) | HR `compression_policy.rs` |
| **LaneEvent control-plane taxonomy** | named lane events + NudgeTracking + RoadmapId supersession + commit provenance (beyond RuntimeEvent) | CC `lane_events.rs` |
| **MCP hardened 11-phase lifecycle + McpErrorSurface** | typed phase FSM + per-phase error surface (configload→…→cleanup) | CC `mcp_lifecycle_hardened.rs` |
| **Provider-prefix routing** | model-name prefix (`openai/`/`local/`/`qwen/`/`kimi/`/`grok`) wins over auth-sniff; foreign-provider hint | CC `providers/mod.rs` |

## Part 2 — 🟦 BREADTH catalog (phase-2 packages; not in core spec)

### Providers (matrix)
- **30–67 provider adapters** across sources (Anthropic, OpenAI(+responses), Google(+Vertex), xAI, Mistral, Groq, Bedrock, Azure, DeepSeek, Moonshot/Kimi, Qwen, OpenRouter, Ollama, vLLM, Cerebras, Copilot, Codex, Cloudflare/Vercel gateways, HuggingFace, …). *OC `extensions/*`, OH `ai/registry`, HE `providers/*`.*
- **Reasoning-model handling**: strip temp/top_p for fixed-sampling; `reasoning_content` history preservation (DeepSeek); thinking budgets per level. *CC, PI.*
- **Embeddings providers**: Voyage/Cohere/Ollama/OpenAI/Custom + rate-limit/retry. *OU `embeddings/`.*
- **Intelligent routing**: health-probe + transparent local↔remote fallback + quality telemetry. *OU `routing/`.*

### Channels & gateway (~30 adapters)
- Telegram, Slack, Discord, WhatsApp(+Cloud), Signal, iMessage, Matrix, MS Teams, Email(IMAP/SMTP), IRC, Line, Mattermost, WeCom, DingTalk, Feishu/Lark, Google Chat, QQ, Yuanbao, WeChat, BlueBubbles, Webhook, ApiServer, Nostr, Relay, … + **setup-wizard**, **dm/group policy**, **pairing/persisted-auth**, **draft-streaming + typing + ack/status reactions**, **message-action discovery**. *HE `gateway/platforms/*`, OC `channels/`.*
- **Cron**: scheduler abstraction (in-process + chronos), atomic claim/TTL, `on-exit` trigger, delivery modes (noop/announce/webhook), blueprints catalog, failure-alert + run-log. *HE `cron/`, CC `team_cron_registry.rs`, OC `cron/`.*
- **ACP (Agent Client Protocol) translator + control-plane**: session lineage, replay, persistent-bindings, permission-relay, spawn external agents (Codex/Claude). *OC `acp/`.*
- **Crestodian sub-agent**: rescue/setup/audit/operations override. *OC `crestodian/`.*
- **Gateway control plane**: scale-to-zero, restart-loop-guard, cgroup-cleanup, memory-monitor, shutdown-forensics. *HE `gateway/`.*

### Voice / media / multimodal
- **TTS** (10+): edge/elevenlabs/openai/minimax/xai/mistral/gemini/piper…; **STT** (6+): faster-whisper/groq/openai/mistral/xai/elevenlabs; **Voice mode** (push-to-talk + continuous + silence-detect); **Talk** (WebRTC + provider-WS + relay + managed-room + audio codec). *HE `tts/stt/voice`, OC `talk/`, OU `voice/`.*
- **Image-gen** (6: fal/krea/openai/openrouter/xai/codex); **Video-gen** (fal/xai + edit/extend); **Vision tools** (analyze + SVG rasterize + native media path). *HE, OH `image/`.*
- **Browser** (CDP + cloud: browserbase/browser_use/firecrawl + Camofox); **screen-intelligence** (macOS capture→vision→persist); **accessibility automation** (AX/UIA/vision_click). *HE `browser_tool.py`, OU `screen_intelligence/`+`accessibility/`.*
- **Media transcode**: ffmpeg + PDF/DOCX/PPT/XLSX/EPUB extract + image audio capability. *OC `media/`, OV parsers.*

### Memory backends (beyond core stub)
- **QMD engine, LanceDB, Active, Wiki** (OC); **Honcho, mem0, supermemory, hindsight, RetainDB, Byterover, OpenViking, Holographic(VecHR)** (HE); + **memory entities** (Obsidian md), **conversations** (JSONL), **graph** (co-occurrence), **tools** (tool-scoped learnings). *OC `memory-*`, HE `plugins/memory/`, OU `memory_*`.*
- **Memory queue** (async jobs: extract/seal/topic_route/digest), **memory sources registry**, **auto-extract on session end**. *OU `memory_queue/`, OV.*

### Dev tools
- **LSP service** (12+ servers: pyright/gopls/rust-analyzer/clangd/tsserver/…; auto-install; workspace + diagnostics + range-shift on edit). *HE `agent/lsp/`, OH `lsp/`.*
- **DAP debugger** (28 ops: launch/attach/step/breakpoints/variables/threads/reverse-requests). *OH `dap/`.*
- **Codegraph** (file-relevance BM25+dense+RRF, content-addressed by git SHA). *OU `codegraph/`.*
- **Code-exec bridge** (py/bun/jl/rb kernels calling back into agent tools; sandbox modes docker/modal/daytona/ssh). *OH `runtime_*`, HE `code_execution_tool.py`.*
- **Git tools** (status/diff/log/show/blame structured); **REPL/PowerShell**; **NotebookEdit**. *CC `Git*`.*
- **Skills ecosystem** (60+ bundled: github/research/mlops/creative/productivity/apple/smart-home/…); **skill AST audit + guard**; **learning graph**; **skill preprocessing** (`{{var}}` + inline shell); **skills-hub install** (GitHub App + JWT). *HE `skills/`, OU `skills/`.*

### Integrations
- **Composio** (1000+ OAuth apps: Gmail/Slack/Notion/GitHub/Calendar + trigger subscriber + OAuth handoff). *OU `composio/`.*
- **HomeAssistant, Spotify, Google Meet, MS Graph, Feishu doc/drive, Notion, Airtable, Google Workspace, Obsidian, Apple (notes/reminders/findmy/imessage), Linear, n8n, Unreal** (optional MCPs). *HE `skills/`, OU.*
- **Kanban** (DB + worktree + dispatch + watchers). *HE `kanban_tools.py`.*
- **Web search/extract** (7-8 engines: tavily/exa/parallel/firecrawl/searxng/brave/ddgs/xai). *HE `web_tools.py`, OC.*
- **OSV vulnerability check** (PyPI `name==version` → OSV.dev). *HE `osv_check.py`.*

### Observability (core-ish)
- **Event-loop health** (delayP99 + utilization + cpuCoreRatio + reasons). *OC `channels.ts`.*
- **OpenTelemetry** plugin (OTLP traces+metrics+logs → Grafana/Datadog/Honeycomb); **Langfuse** export. *OC `diagnostics-otel/`, HR.*
- **Stuck-session recovery** (5min stale, 15s abort-settle, in-flight tracker); **retry-hint policy**; **failover-error classification**. *OC `logging/diagnostic-*`.*
- **Cache-trace JSONL** (opt-in); **proxy-capture** (TLS-MITM replay + coverage). *OC.*
- **Cost/billing/credits/rate-limit** tracking (per-provider usage snapshots, header-parse credits, monthly cap/auto-reload). *HE `billing_view.py`, OU `cost/`.*
- **UnifiedLogger** (bounded queue + async flush + rotation + drop counter); **withLogContext ALS** (correlation auto-injected). *MA.*
- **TokenJuice/Kompress** (vendored TinyJuice + ML plain-text compressor). *OU `tokenjuice/`.*

### Security / supply (core-ish)
- **Secrets**: OS-keyring (macOS Keychain/Win CredMgr/Linux Secret Service) + sealed-0600 file fallback + `SecretRef` contract + `secrets.resolve` wire + audit redaction + per-channel envVar schema. *OC `secrets/`, OU `keyring/`.*
- **Device pairing** (X25519+XChaCha20 tunnel; setup-code/QR; token rotate/revoke); **Push** (APNS + WebPush + VAPID + silent_push + relay/direct). *OC, OU `devices/`.*
- **Plugin-approvals + exec-approvals** (per-surface allowlists, per-node overrides, snapshot). *OC.*
- **Sigstore npm provenance** (post-publish verify) + transitive-manifest-risk-report + vulnerability-gate + import-cycle/loc/architecture-smell lints. *OC `scripts/`, MA.*
- **Proxy policy** (provider-aware env injection; `NO_PROXY` localhost protection; `applyProviderProxyPolicyToEnv`). *MA `proxy_config.rs`.*

### Product / platform / UX
- **Desktop** (Electron/Tauri); **mobile** (devices pairing, iOS/Android apps); **daemon** (launchd/systemd/schtasks); **web dashboard** (FastAPI/Vite + basic-auth/OAuth). *HE `apps/desktop`, OC `apps/`, OU `service/`.*
- **Pet companion** (sprite-sheet renderer: Kitty/iTerm2/Sixel/Unicode + AI sprite gen); **autocomplete**; **i18n** (YAML locales); **themes** (JSON, 51-color schema, hot-reload, light/dark auto-detect); **prompt-templates** (`$1`/`$@`/`${N:-default}`); **HTML/JSONL export**; **pi-share-hf** (HuggingFace session publish); **`/share` gist**; **diffs viewer**; **workboard/thread-ownership**. *HE, PI, OC.*
- **Update channel** (signed releases + appcast.xml); **migration tools** (migrate-claude/hermes); **install telemetry** (opt-out + provider attribution headers). *OC `update.ts`, PI.*
- **Tinyflows + tinyplace + orchestration** (workflow engine seam + A2A social network + tiny.place session DMs). *OU.*
- **x402/web3/wallet** (Solana/EVM dapps + deBridge + HTTP-402 micropayments + Ed25519 signing). *OU.*

## Part 3 — recommendation
- **R31–R33:** fold Part 1 (🟥 CORE, ~27 items) into the spec — these are the load-bearing gaps. Prioritize: ToolSearch, path-safety resolver, session JSONL tree, project-trust, message-queue, provider compat-flags, auth-profile rotation, readiness 3-phase, unified cancel, large-value spill, Approval-Token ledger, RecoveryRecipe/GreenContract, MCP 11-phase, LaneEvent taxonomy.
- **Phase 2+ packages:** Part 2 (🟦) — each category becomes installable packages per the §17 model; NOT in core spec. The agent ships minimal; users `install` channels/voice/dev-tools/integrations as needed.
- **The core spec (spec/00–11) stays lean** — it defines interfaces; packages implement. This file is the "nothing-forgotten" catalog + sourcing.

## Part 4 — R32 verification deltas (breadth quirks deep-read confirmed)

R32 deep-verified providers/channels/dev-tools. Notable breadth features the Part-2 lines under-specified (each = a phase-2 package with a known source):

**Providers (adapter quirks beyond "compat flags ~20"):** Codex app-server fleet supervisor (MCP + policy gates) · Copilot BYOK loopback-proxy transport + replay-shim fingerprint + SDK session pool (idle TTL) · Bedrock app-inference-profile ARN→GetInferenceProfile + cache-point injection · Bedrock-Mantle (OpenAI-compat variant) · Anthropic-Vertex (Claude on GCP, ADC) · Anthropic 4.7/4.8 native xhigh/max effort + 1M ctx + 128K out · ClawRouter credential-scoped catalog cache + $-budget billing snapshot · OpenRouter→Mistral strict9 tool-call IDs · OpenRouter Fusion/Pareto-code payload gates + x-grok-conv-id pinning · OpenRouter cache-TTL eligibility allowlist · DeepSeek V4 thinking+reasoning_effort (V3 untouched) · Kimi/OpenCode-Go/MiniMax **thinking XOR reasoning_effort** (mutually exclusive) · Xiaomi MiMo rejects list-type tool msgs · MiniMax-M3 reasoning_split + CN/oauth profile split · OpenRouter→Claude-4.6+ verbosity-only reasoning (drop `reasoning` field) · Codex device-code OAuth (403/404=pending, 120 polls) · GitHub Copilot bulk policy-enable dance (POST per model) · Claude-Code UA bootstrap for legacy creds · GitLab Duo vscode:// redirect + stall-restart quotas. *(openclaw extensions/, oh-my-pi ai/registry, hermes plugins/model-providers, openhuman inference)*

**Channels/gateway (beyond "~30 adapters + dm/group policy"):** ChannelSecretsAdapter SDK (`{source:env|file|exec}` runtime contract) · OperatorScope closed-set (admin/read/write/approvals/pairing/talk.secrets) · ChannelApprovalNativeAdapter (origin vs approver-DM dual surface) · ChannelAllowlistAdapter (group-vs-DM edit contract) · ChannelRuntimeSurface SDK (reply/routing/media/commands/groups/pairing) · stateful target drivers (lazy, ACP-bound) · conversation-bindings provider (placement current/child) · **AcpEventLedger** (SQLite, 200 sess/5000 evt/16MiB, replay) · ACP approval-classifier taxonomy (readonly_search auto-approves) · ACP permission-relay (Gateway↔ACP bidirectional) · ACP triple-gate policy (enabled/dispatch.enabled/allowedAgents) · ACP backend-failover + background-task progress streaming · **Talk event FSM** (28 typed events: session/turn/capture/input.audio/transcript/output.audio/tool/usage/latency/health) + TalkBrain (agent-consult/direct-tools/none) + audio codec matrix (G.711 μ-law / PCM16 24kHz) · **Relay connector protocol** (frozen CapabilityDescriptor v1, multi-platform-per-adapter) + HMAC-SHA256 upgrade-bearer + delivery-signature (300s skew, multi-secret rotation) · **Webhook HMAC** (GitHub/GitLab/Svix/Stripe, V2 timestamp-bound supersedes V1, profile multiplexing `/p/<profile>`, dynamic routes) · Yuanbao pb-proto + COS upload + sticker-cache file_unique_id · Cron `on-exit` 4th kind (nextRunAtMs undefined; watcher-driven) · CronSchedulerProvider ABC + chronos external slot + is_available() fallback · Cron SQLite quarantine sidecar (invalid-config retention) · Cron delivery prefix-routing (`slack:C123`) + per-channel validation · Cron direct-delivery (isolated-agent→channel core, bypass run-context) · Crestodian chat-engine + TUI/gateway-RPC dual surface + rescue-policy (YOLO+ownerDm+sandbox gates) · Node-Connect `qr --json` + 4-source URL topology (LAN/Tailscale/public/remote) · message-action discovery w/ visibility+mediaSourceParams · direct-DM ingress (legacy compat + pre-crypto guard). *(openclaw channels/+acp/+cron/+talk/+crestodian/, hermes gateway/, openhuman channels/)*

**Dev-tools (beyond "LSP/DAP/codegraph/code-exec"):** LSP CLI (`hermes lsp status/list/install/restart/which`) + broken-set backoff (skip failed spawns) + eventlog dedup + **git-worktree-only gate** + per-server exclude-marker (deno beats tsserver) + tsserver seed-first-push + server-pull handlers (workspace/configuration, applyEdit, refresh family, showMessageRequest) + bidirectional writethrough (didOpen/didChange pre-write, inline 500ms vs deferred 12s diagnostic wait) + rust-analyzer workspace-ready progress probe + rename_file cross-server coalesce (≤1000 pairs) + `lsp request` arbitrary-method passthrough + `*` workspace diagnostics (cargo/tsc/go/pyright autodetect) + lspmux instance-sharing + custom linter clients (Biome/SwiftLint CLI vs LSP) · **DAP 27 session ops + 4 lifecycle accessors** (not 28) + socket-mode (Linux unix / macOS TCP) + detach+setsid debuggee isolation + reverse-requests (runInTerminal/startDebugging/capability-reg) + configurationDone race-resolution + breakpoint mutation queue (serialize) + 128KB output trim + 10min idle cleanup + stop-outcome race mitigation + debugpy-missing-module hint + 14 default adapters (ext→root-marker→rank) · **Codegraph 3-schema** (blob(sha)+manifest(repo,ref)+GC) + auto size-gate (dense_min_files=400) + structural-doc heuristic extractor + identifier-splitting tokenizer + Coverage flag (Full/Partial/None) + RRF top-20/arm k=60 + path-canonicalization anti-traversal · **Code-exec bridge**: loopback HTTP+bearer (`/v1/tool`, abort-prompt-on-abort) + 5 synthetic helpers (`__agent__`/`__completion__`/`__budget__`/`__concurrency__`/`tool.<n>`) + timeout-pause/resume events + (sessionId,runId) namespacing + local UDS↔TCP fallback + remote file-RPC (docker/modal/daytona/ssh/singularity) + env-scrub (`_SECRET_SUBSTRINGS` + `HERMES_*` allowlist + max_tool_calls audit) + 4 backends (py/js/jl/rb) on shared kernel-base · Git tools (5: status/diff/log/show/blame + `commit:path` + filters) · REPL/PowerShell (DangerFullAccess + run_in_background + classifier) · NotebookEdit (edit_mode replace/insert/delete + cell_type) · Agent/Skill tools (Skill has args; Agent has name+model+subagent_type). *(hermes lsp/, oh-my-pi lsp/+dap/+eval/, openhuman codegraph/, claw-code tools/)*

## Part 5 — R33 verification deltas (memory/voice/integrations/observability/security/product)

R33 deep-verified the remaining 6 categories. Notable breadth + **corrections** (⚠️ = fixes a wrong Part-2 claim):

**Memory backends (concrete mechanics):** memory-queue **6 jobs** (extract_chunk/append_buffer/seal/topic_route/digest_daily/flush_stale; 3-worker pool + daily scheduler + 5-min claim lease + dedupe partial-unique index) · memory-sources **7 kinds** (composio/folder/github/rss/twitter/web_page; per-kind SourceReader; sync-status Active≤30s/Recent≤5min/Idle; 10MB folder cap; path-traversal guard) · memory-tools (tool-scoped `tool-{name}` namespace; ToolMemoryPriority Normal/High/Critical + capture hook; Critical/High pinned into system prompt) · memory-entities (Obsidian md: YAML frontmatter + atomic upsert + EntityHandle; migrated from SQLite people/) · memory-conversations (2-file: threads.jsonl + threads/<id>.jsonl; process mutex; append-only, no embeddings) · memory-graph (read-only overlay over entity index; weight=distinct shared nodes; NOT triple-extraction) · **memory-wiki 3 vault modes** (isolated/bridge/unsafe-local) · memory-lancedb (autoCapture/autoRecall + customTriggers + captureMaxChars + `${ENV}` interpolation) · **active-memory blocking sub-agent** (30+ config: allowedChatTypes/Ids, thinking, queryMode, promptStyle, circuitBreaker, qmd.searchMode, cacheTtl, persistTranscripts) · **memory-core dreaming 3-phase** (light/REM/deep w/ distinct knobs + Dream Diary subagent + model-override) · **Holographic HRR** (SHA-256 atoms; bind/unbind/bundle phase ops; encode_text/encode_fact; snr_estimate warn <2.0; phase-encoded not complex) · **headroom CCR 6 modules** (response_handler max_retrieval_rounds=3 + batch_store + batch_processor + mcp_server + context_tracker w/ ExpansionRecommendation threshold 0.3 + tool_injection). ⚠️ **CORRECTION**: memory-embed providers = 9 (lmstudio/bedrock/voyage/ollama/mistral/deepinfra/github-copilot/openai/gemini) — **no Cohere** in code.

**Voice/media/multimodal (counts corrected):** ⚠️ **TTS = ~27** (10 hermes builtin + 17 openclaw speech); per-provider char caps (edge 5k/openai 4k/xai 15k/gemini 32k/elevenlabs 10k + model-specific) · ⚠️ **STT = 12** (7 hermes incl. unlisted elevenlabs + 5 openclaw realtime) + command-provider registry (`{input}/{output}/{lang}`, 300s, txt/json/srt/vtt) · ⚠️ **Image-gen = 17** (11 OC + 6 HE) + FAL 9-model catalog · ⚠️ **Video-gen = 17** (15 OC + 2 HE) · ⚠️ **MUSIC-gen = NEW CATEGORY** (5 OC: minimax/comfy/openrouter/google/fal incl. ComfyUI workflow) · **Voice-call** (Twilio/Telnyx/Plivo+mock; 4 tunnel modes; per-number E.164 routing; sessionScope; tts modelOverrides perms; webhook-security) · ⚠️ **Talk = provider-websocket LIVE** (openai/google only; webrtc/managed-room are theoretical enum; audio = G.711 μ-law 8kHz + PCM16 24kHz ONLY, no Opus; 28 events; TalkBrain agent-consult/direct-tools/none; consultPolicy + fastContext) · **Openhuman voice**: dictation server (rdev Globe/Fn + cpal 16kHz); **Oculus-15 viseme** lip-sync; provider slugs; macOS hotkey safety (CGEventTap EXC_BREAKPOINT on macOS 26 → core no-ops) · **screen_intelligence** (macOS-only; 3-pass OCR: Apple Vision→vision-LLM→synthesis; drain-to-latest dedup; permission cache; ring buffer 120; TTL 30..3600s; Globe/Fn) · **accessibility** (Swift Process-Bridge for AX FFI; Windows UIA; `automate` multi-step FSM + step-budget + stuck-detection; `vision_click` guarded frontmost-only) · ⚠️ **MISSING modules**: meet_agent (live Meet STT/LLM/TTS loop + VAD + wake-word FSM + barge-in + 16kHz hard-reject) · desktop_companion (Clicky-style Idle→Listening→Thinking→Speaking/Pointing FSM + multi-monitor point-map + provider-surface handoff) · audio_toolkit (podcast gen + email-podcast, format/provider coupling) · codex Computer Use + codex-supervisor (app-server harness + sandbox/approvalPolicy + network proxy + codex_threads tool).

**Integrations:** ⚠️ **Composio = 31 curated** (4 native providers gmail/notion/github/linear + 27 catalog-only; "1000+" is backend raw) · OAuth handoff = **Meta-only** guard (POST-clean stale + 3-attempt backoff on 429 for instagram/facebook) · trigger subscriber = **2-tier LLM triage** (drop/acknowledge/handle/escalate; opt-out flag) · direct-mode loses async triggers (ComingSoon; poll 20min) · direct-auth circuit-breaker (consecutive-401 by sha(api-key)) · connection-readiness polling (60s exp-backoff) · trigger-history = daily JSONL + fs2 file-lock · Kanban = 5-component (SQLite + systemd dispatcher + worker-spawn + non-blocking dispatch lock + gateway watcher) + worker task-ownership enforcement + goal-judge fail-open + workspaces (scratch/worktree/dir) · HomeAssistant = 2 layers (toolset + platform adapter; HE not OC) · ⚠️ Linear/n8n/Unreal = HE `optional-mcps/` (NOT OC extensions) · Spotify = HE toolset (off-by-default) · Discord = 15 actions × core/admin · Project tools = GUI-only · Clarify gateway = dual-delivery + agent-thread heartbeat (watchdog touch) · send_message target-ref grammar (`platform`/`platform:#chan`/`platform:chat`/`platform:chat:thread`) · session_search = profile+sessionId+anchor modes.

**Observability:** Health registry **4-state** (healthy/degraded/critical_unhealthy/degraded_components; CRITICAL_COMPONENTS=[core,memory_tree_db] only) · event-loop liveness **7 metrics** (delayP99/Max + utilization + cpuUser/System/Total/coreRatio + reason enum) · OpenTelemetry = separate OC plugin (`diagnostics-otel/`, OTLP http/protobuf, traces/metrics/logs gates, 128KB cap) · ⚠️ **Langfuse = HR-only** (headroom/observability/tracing.py; not OC) · cache-trace = opt-in + 3 include flags + 11 stages + sha256 fingerprints · ⚠️ **proxy-capture = HTTP-proxy (env-injected), NOT TLS-MITM** (per-child env + optional CA dir + coverage report) · stuck-session recovery = **4 actions** (observe_only/keep_lane/release_lane/abort_embedded_run; 5min stale/15s settle; in-flight dedup) · retry-hint = cron-specific (429/529/timeout/network/server_error) · HealthSubscriber reacts to domain events (SystemStartup/HealthChanged/ChannelConnected/Disconnected) · cost tracker (per-session UUID, `<workspace>/cost/cost.jsonl`, check_budget) · HE billing = Decimal-strict + idempotency-key + fail-open · HE account_usage = 3-provider fetchers (codex/anthropic/openrouter) unified snapshot · HE credits-tracker = strict-header parse (`x-nous-credits-v1` micros + 10/25/90% thresholds) · HE rate-limit-tracker = `x-ratelimit-*` buckets (req_min/hour + tokens_min/hour + usage_pct) · MyAgents UnifiedLogger (≤1000 queue + 100ms flush + 50MB rotation + 200-line ring + `[NODE ]` bun-compat + correlation tag) · logger-context (ALS + per-owner ambient map; 4 generation points) · DeferredInitState (pending/phase/ready/failed state-machine; 3 endpoints /health/{live,ready,functional} + gate-body mirror) · large-value store (≤256KiB inline else refs/<id> + 8KiB preview + 1h TTL + 60s clearExpiredRefs).

**Security/supply + product/platform:** ⚠️ **Device-pairing crypto richer**: X25519 + **HKDF-SHA256 directional subkeys (c2s/s2c)** + frame v2 + 128-slot nonce replay window + cross-direction reflection defense · ⚠️ **OS keychain = 2 distinct subsystems**: OC = `SecretRef{env|file|exec}` providers (NO native keyring); OU = ChaCha20-Poly1305 encrypted-file backend (master key in OnceLock, single OS call, fail-closed on keyring error #3311) · ⚠️ **audit-redaction = OU `approval/redact.rs`** (sensitive-key walk + `<HOME>` scrub), NOT OC secrets · plugin-approvals = 2-phase per-reviewer (approvalReviewerDeviceIds, 600s, severity, decisions) · exec-approvals = systemRunPlan (argv/cwd/mutableFileOperand{argvIndex,path,sha256} + commandSpans UTF-16 + per-node baseHash lock + socket config) · ⚠️ **x402 = Ed25519 (Solana SPL) AND EVM EIP-3009 secp256k1** (Base/Ethereum; facilitator co-signs fee-payer) — not Ed25519-only · ⚠️ **wallet = ETH/Base/Arbitrum/Optimism/Polygon + BTC P2WPKH + Solana + Tron** (swap-broadcast is quote-only) · deBridge + RPC-URL redaction (scheme://host before logging) · ⚠️ **MyAgents proxy fns = `apply_to_subprocess`/`apply_to_subprocess_for_provider`** (not `applyProviderProxyPolicyToEnv`) + 4 pit-of-success wrappers (proxy_config/local_http/process_cmd/system_binary) · cross-process file-lock = atomic-mkdir + owner token + **pid-reuse defense** (Linux /proc/pid/stat field-22 + macOS ps lstart) · cancellation = CancelReason enum (user/timeout/upstream/shutdown/error) + bounded-cancel invariant · ⚠️ **pi themes = NO hot-reload** (51-color schema + light/dark auto-detect are accurate; hot-reload is OC config/plugins only) · ⚠️ **tinyflows = capability-seam adapter**; the actual workflow engine is `openhuman/flows/` (Flow/FlowConnection/FlowRun/FlowRunStep + n8n-import + run-registry + cron-style runs) · ⚠️ **tinyplace DMs = Signal-protocol E2E** (double-ratchet; SLIP-0010 seed from Solana wallet) · update signing = Sparkle ed25519 `edSignature` via `make_appcast.sh` (not gateway update.ts).

## Part 6 — structural surface index (R34; names only — "nothing-forgotten" at module granularity)

Category-level entries above cover these conceptually; this index lists the concrete per-source surfaces so no module/crate/command is silently dropped. (Phase-2 package candidates.)

**claw-code:** `rust/crates/commands/` = **70+ slash commands** (Advisor, Bughunter, Teleport, Ultraplan, DebugToolCall, Doctor, Stickers, Insights, Thinkback, Brief, SecurityReview, Review, Branch, Rewind, Voice, Effort, OutputStyle, AddDir, History, Team, Tasks, Hooks, Context, Color, Ide, Tag, Copy, Rename, Fast, Files, Feedback, Share, Stats, Upgrade, Memory, Init, Diff, Clear, Cost, Session, Export, Config, Resume, Setup, Vim, Login, Logout…) · runtime/src modules: bash_validation, branch_lock, compact, conversation, file_ops, g004_conformance, git_context, hooks, permission_enforcer, permissions, plugin_lifecycle, policy_engine, prompt, remote, sandbox, session_control, stale_base, stale_branch, summary_compression, task_packet, task_registry, trident, trust_resolver, usage, worker_boot, lsp_client, report_schema, sse · mcp_{,client,server,stdio,tool_bridge,lifecycle_hardened}.rs · tools: lane_completion, pdf_extract, 5 Git + REPL/PowerShell/NotebookEdit/Agent/Skill · **`src/` = parallel Python port** (assistant/cli/skills/plugins/server — uncatalogued; confirm if maintained).

**oh-my-pi:** packages/ = agent, catalog, collab-web, hashline, mnemopi, natives, snapcompact, stats, swarm-extension, terminal-bench, tui, typescript-edit-benchmark, utils, wire (+ coding-agent, ai) · crates/pi-* = pi-ast, pi-iso, pi-natives, pi-shell, pi-uu-grep, pi-uutils-ctx, pi-walker (+ vendor brush/uutils) · coding-agent/src/ subsystems: advisor, async, auto-thinking, autolearn, autoresearch, capability, cli, collab, commands, commit, config, cursor, debug, discovery, edit, exa, exec, export, extensibility, goals, hindsight, internal-urls, irc, jsonrpc, lib, markit, mcp, memories, memory-backend, mnemopi, modes, plan-mode, prompts, registry, secrets, session, slash-commands, ssh, stt, subprocess, task, tiny, tool-discovery, tools, tts, tui, utils, web.

**openhuman:** about_app, agent_experience, agent_meetings, agent_tool_policy, agentbox, agent_orchestration, app_state, artifacts, connectivity, doctor, heartbeat, monitor, startup, credentials, encryption, keyring_consent, cwd_jail, http_host, mcp_audit, mcp_client, mcp_registry, mcp_server, prompt_injection, provider_surfaces, webview_{accounts,apis,notifications}, whatsapp_data, subconscious, threads, thread_goals, todos, session_db, session_import, service, tls, file_state, team, referral, redirect_links, overlay, council_registry, routing, embeddings, profiles, scheduler_gate, learning, tool_status, tool_timeout, update + src/{core,api,rpc,bin}.

**openclaw:** packages/ = markdown-core, net-policy, normalization-core, terminal-core, tool-call-repair, model-catalog-core, acp-core, plugin-sdk, speech-core, web-content-core, media-understanding-common, plugin-package-contract, memory-host-sdk, gateway-{protocol,client}, sdk · extensions/ extras = admin-http-rpc, bonjour, clickclack, file-transfer, open-prose, phone-control, raft, + 30+ provider adapters · apps/ = swabble (Speech.framework wake-word), macos-mlx-tts.

**hermes-agent:** hermes_cli/ (100+ subcommands), tui_gateway/, optional-skills/ (17 categories: autonomous-ai-agents/blockchain/finance/gaming/health/mcp/devops…) · plugins/ extras: cron_providers, browser, context_engine, kanban, image_gen, video_gen, dashboard_auth, disk-cleanup, google_meet, teams_pipeline, spotify, web, observability, security-guidance, hermes-achievements · gateway/platforms extras: qqbot, weixin, msgraph_webhook, signal_format, yuanbao_media + plugins/platforms: photon, raft, simplex, sms, ntfy.

**pi-coding-agent:** src/bun/ (photon-WASM resize, register-bedrock, restore-sandbox-env), src/cli/ (startup UI), modes/{print-mode,rpc/}, core/{export-html, auth-storage, auth-guidance, event-bus, telemetry, system-prompt, messages, cache-stats, output-guard, provider-attribution}, docs/ (28 — only custom-provider.md was referenced; 27 more: extensions/skills/providers/models/sdk/sessions/session-format/settings/keybindings/themes/json/prompt-templates/security/shell-aliases/terminal-setup/termux/tmux/tui/usage/windows/packages/quickstart/rpc/development/containerization/compaction), scripts/migrate-sessions.sh.

**headroom:** subcrates = headroom-core, headroom-parity, headroom-proxy, headroom-py · headroom-core/src/ = ccr/, relevance/ (bm25/embedding/hybrid), signals/ (keyword_detector/line_importance/tiered), tokenizer, transforms, onnx_cpu, auth_mode, cache_control, compression_policy · wiki/ (36 docs).

**OpenViking:** openviking/ modules uncatalogued: crypto, message, metrics, privacy, pyagfs, web_studio, observability, telemetry, ingest, integrations, prompts, eval (+ ragfs Python SDK, SDK packages, bot/, benchmark/).

**MyAgents (most under-catalogued):** src-tauri/src/ = attachment_protocol, browser, cron_task, device_identity, floating_ball(+pets), global_shortcut, i18n, im/, inbox/, legacy_upgrade, litellm_cache, macos_arrow_filter, macos_traffic_light, managed_codex, management_api, notification(+badge), perf_trace, process_cleanup, process_cmd, search/, session_metadata, sidecar/, space_cloud(+mock), sse_proxy, system_binary, task, terminal, thought, tray, updater, wake_lock/ · src/server/ = agents/, analytics, bridge-cache, builtin-session/, dir-info, inbox/, mcp-oauth, migrations/, official-tools, openai-bridge, plugin-bridge, plugins/, provider-probe, provider-verify, routes/, runtimes/, services/, session-core/, session-engine, SessionStore, skills/, sse(-instance), subscription-auth, system-prompt, title-generator, tools/, turn-hooks, types/.

**Stale-ref corrections (Part 1/4 path drift):** PI `bash.ts`/`file-mutation-queue.ts`/`compaction/` actually under `core/tools/`+`core/compaction/` (not flat) · CC `preflight_message_request` is a symbol in session.rs/compact.rs (not a file) · `claw-code/commands` + `claw-code/SPEC` do NOT exist (commands = `rust/crates/commands/`; no SPEC dir — closest is docs/ + prd.json/ROADMAP/concept).
