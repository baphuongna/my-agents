import { Card } from "@/components/ui/Card"; import { PageHeader } from "@/components/PageBits"; import { PawPrint } from "lucide-react";
const pets = [{ name: "Byte", icon: "🐕", unlocked: true }, { name: "Kernel", icon: "🐈", unlocked: true }, { name: "Nimbus", icon: "🦊", unlocked: false }, { name: "Patch", icon: "🐼", unlocked: false }];
export function PetsPage() { return <div className="p-4 max-w-4xl mx-auto space-y-3"><PageHeader title="Petdex" icon={PawPrint} /><p className="text-sm text-fg-muted">Collect companions as you explore and complete tasks.</p><div className="grid grid-cols-2 sm:grid-cols-4 gap-3">{pets.map(p => <Card key={p.name} className={p.unlocked ? "text-center" : "text-center opacity-50 grayscale"}><div className="text-4xl">{p.unlocked ? p.icon : "❔"}</div><h2 className="font-semibold mt-2">{p.unlocked ? p.name : "Locked"}</h2><p className="text-[10px] uppercase text-fg-subtle">{p.unlocked ? "Unlocked" : "Keep exploring"}</p></Card>)}</div></div>; }
