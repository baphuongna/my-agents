import { describe, it, expect } from "vitest";
import * as mod from "./format.js";
describe("[smoke] format (covered by web-utils.test.ts)", () => { it("module loads", () => { expect(mod).toBeDefined(); }); });
