import { describe, it, expect } from "vitest";
import * as mod from "./reasoning-effort.js";
describe("[smoke] reasoning-effort (covered by web-utils.test.ts)", () => { it("module loads", () => { expect(mod).toBeDefined(); }); });
