import { Button } from "@/components/ui/Button"; import Terminal from "@/components/Terminal"; import { MODAL_BACKDROP, MODAL_PANEL } from "@/lib/modal-constants";
export interface ConsoleModalProps { open: boolean; onClose: () => void }
export default function ConsoleModal({ open, onClose }: ConsoleModalProps) { if (!open) return null; return <div role="dialog" aria-modal="true" aria-label="Console" className={MODAL_BACKDROP}><div className={`${MODAL_PANEL} w-full max-w-4xl`}><div className="flex justify-between items-center mb-3"><h2 className="font-semibold">Console</h2><Button size="sm" onClick={onClose}>Close</Button></div><Terminal /></div></div>; }
