import { describe, it, expect } from "vitest";
import * as mod from "./diff.js";
describe("[smoke] domains/diff (covered by domains.test.ts)", () => { it("loads", () => { expect(mod).toBeDefined(); }); });
